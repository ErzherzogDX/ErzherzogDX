# 👋 welcome to the space 👋

## Учебные проекты

### [Интерпретатор Python на Python](https://github.com/ErzherzogDX/python-interpreter) (python)
 1) Реализовал виртуальную машину, которая исполняет Python-байткод: расширял каркас в ```vm.py```, сохраняя интерфейс ```VirtualMachine.run```, чтобы пройти максимальное число тестов ```test_public.py``` (тесты разбиты по уровням сложности операций).
 2) Ключевая сложность — воспроизвести семантику CPython на стеке: корректно обрабатывать операции  ```dis``` (работа со стеком, переменные/области видимости, контроль потока, структуры данных, вызовы функций и т.д.), ориентируясь на документацию ```dis``` и поведение CPython.

### [Мини-библиотека базовых алгоритмов оптимизации](https://github.com/ErzherzogDX/optimization-methods-lib) (python) 
  1) **Метод Ньютона для задачи с ограничениями**. Реализация метода Ньютона, который на вход получает объект со значением функции, градиентом и Гессианом. Для устойчивости делал перезапуски из разных стартовых точек, т.к. метод может сходиться в том числе к седловым точкам. Демонстрация на функции Розенброка с нелинейным ограничением y = −x^2, оптимизация ведётся по Лагранжиану. 
  2) **Градиентный спуск**. Реализовал градиентный метод для constrained-задачи через минимизацию ||∇L|| (нормы градиента Лагранжиана), с возможностью перезапуска из разных точек. Демонстрация на той же функции Розенброка и ограничении y = −x^2. 
  3) **L-BGFS**. Реализовал L-BFGS, который принимает функцию, градиент и параметр m — число шагов истории (сколько пар (s_k, y_k) хранить). Демонстрация на N-мерной версии функции Розенброка (с параметром N). 
  4) **Градиентный спуск с линейными ограничениями**. Реализовал градиентный спуск с фиксированным шагом для произвольной функции при наличии линейных ограничений, заданных как системой уравнений (в явном или матричном виде). Для работы с ограничениями использовал вычисление ядра оператора ограничений (допускалось использовать библиотеки вроде scipy/Eigen). Демонстрация на функции Розенброка с линейным ограничением вида y − x = 3. 
  5) **Генетический алгоритм для задачи коммивояжера**. 
  6) **Симплекс-метод для задач LP И ILP**. Реализовал симплекс-метод для задач линейного программирования; чтобы избежать зацикливания — выбирал случайные входящие/выходящие переменные из допустимых. Для бинарного случая реализовал метод секущих плоскостей с сечениями Гомори на базе симплекс-решателя. 

### [Пет-проект по детекции жд-вагонов](https://github.com/ErzherzogDX/train-project) (python) 
- Проблема: на сортировочных станциях/терминалах есть поток изображений (инспекции/камеры/отчёты); ручная проверка наличия вагонов и их положения на кадре занимает время и даёт ошибки.
- Цель: автоматически детектировать вагоны на изображении (bounding boxes) и отдавать результат в систему учёта/контроля или в пайплайн последующей аналитики.
- Данные: открытый датасет в YOLO-формате (train/val, bbox-аннотации); проверка структуры датасета и базовый контроль качества.
- Модель: YOLOv8 (transfer learning); настройка гиперпараметров (imgsz/batch/conf/iou), контроль переобучения по валидации.
- Оценка: метрики детекции mAP@0.5 и mAP@0.5:0.95; анализ типовых ошибок (FP/FN) и настройка порогов уверенности.
- Упаковка: обучающий пайплайн + сохранение артефактов (best.pt, meta.json), CLI для инференса и REST API (FastAPI) для интеграции и пакетной обработки.

#### Note: Проекты по CV по просьбе организаторов пока что находятся в приватных репозиториях

### [ШАД Computer Vision]
 1) Реализовал end-to-end JPEG-компрессию с потерями: RGB↔YCbCr, downsampling цветовых компонент, DCT 8×8, квантование (Quality Factor) и декодирование; сравнил влияние параметров на качество/артефакты.
 2) Построил multi-object tracking пайплайн: SSD-детекции по кадрам, ассоциация треков по IoU и поддержание треков во времени, расчёт MOTP/MOTA; ускорил обработку между детекциями через template matching (NCC).
 3) Сделал простой детектор автомобилей: обучил бинарный классификатор и преобразовал его в fully-convolutional детектор (замена Linear на Conv) со heatmap “скользящего окна”, реализовал IoU и постобработку bbox по порогу/фильтрации.
 4) Реализовал задачу регрессии лицевых keypoints (14 точек): пайплайн подготовки данных (resize + пересчёт координат), CNN на PyTorch, обучение/валидация по MSE и анализ ошибок.






<!--
**ErzherzogDX/ErzherzogDX** is a ✨ _special_ ✨ repository because its `README.md` (this file) appears on your GitHub profile.

Here are some ideas to get you started:

- 🔭 I’m currently working on ...
- 🌱 I’m currently learning ...
- 👯 I’m looking to collaborate on ...
- 🤔 I’m looking for help with ...
- 💬 Ask me about ...
- 📫 How to reach me: ...
- 😄 Pronouns: ...
- ⚡ Fun fact: ...
-->
